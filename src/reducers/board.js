import update from "react-addons-update";

     
const initialState = [];

const itemsInitialState = {}
export default function board (state = itemsInitialState, action) {
    switch (action.type) {
        default:
            return state
    }
}