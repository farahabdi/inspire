import { Reducer } from 'redux';
import update from "react-addons-update";

     
const itemsInitialState = {}
export default function items (state = itemsInitialState, action) {
    switch (action.type) {
        default:
            return state
    }
}